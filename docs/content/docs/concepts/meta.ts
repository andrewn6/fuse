import { defineMeta } from "blume";

export default defineMeta({
  title: "Concepts",
  icon: "layers",
  pages: ["fusefile", "providers", "hosts", "environments", "snapshots", "layer-cache", "artifacts", "scheduling", "reconcile", "state-and-recovery"],
});
